# RomM Bridge

A TUI front end that connects to a ROMM server to sync ROMs and ROM metadata with different frontends (like ES-DE).

Currently supports:

- ES-DE

Can be run like so:

`uv run romm_bridge.py`
